user = www-data
group = www-data

; Ensure logs go to stdout/stderr so Docker can capture them
php_flag[display_errors] = off
php_admin_value[error_log] = /proc/self/fd/2
php_admin_flag[log_errors] = on
